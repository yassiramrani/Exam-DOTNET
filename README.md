# Exam-DOTNET

A C#/.NET library-management application organized around models, services, and custom exceptions.

## Run

Open `BibliothequeNumerique.sln` in an IDE or run:

```bash
dotnet run
```

This repository is a fork of `Sayfbennani/Exam-DOTNET`.
